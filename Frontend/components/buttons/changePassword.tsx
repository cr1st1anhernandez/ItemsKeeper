'use client';
import { Button } from '@nextui-org/button';

export const ChangePassword = () => {
  return (
    <Button color="primary" className="w-fit">
      Change Password
    </Button>
  );
};
